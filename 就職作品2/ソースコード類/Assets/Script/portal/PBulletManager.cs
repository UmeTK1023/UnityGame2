using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PBulletManager : MonoBehaviour
{

    public int WarpBulletF;


    // Start is called before the first frame update
    void Start()
    {
        WarpBulletF = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
